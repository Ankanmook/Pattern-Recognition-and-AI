FinalClassifier.py
------------------

usage: python FinalClassifier.py <inkmlfile>

Uses the Random Forest and 1NN classifier trained on all samples to classify 
the given .inkml file.

Outputs resultant .lg files to 'results' directory in truth, forest and knn 
subdirectories.


Note: Takes ~20 seconds to load classifiers, due to size. Sorry.
